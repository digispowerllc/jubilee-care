import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Other config options...
  allowedDevOrigins: ['http://192.168.0.159:3000, http://localhost:3000'],
};

export default nextConfig;
